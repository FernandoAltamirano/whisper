import styled from "styled-components";

export const NewTweetWrapper = styled.section`
  padding-top: 10px;
  min-width: 200px;
  background-color: var(--white);
  & > div {
    border-top: 1px solid var(--gray);
    border-bottom: 1px solid var(--gray);
  }
`;
